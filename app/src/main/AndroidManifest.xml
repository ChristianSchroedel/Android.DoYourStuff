<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.schroedel.doitlater" >

    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >

        <receiver android:name=".service.AlarmReceiver"/>
        <receiver android:name=".service.UpdateItemReceiver">
            <intent-filter>
                <action android:name="de.schroedel.doit.service.UpdateItemReceiver.ACTION_SNOOZE"/>
                <action android:name="de.schroedel.doit.service.UpdateItemReceiver.ACTION_DONE"/>
            </intent-filter>
        </receiver>

        <activity
            android:name=".activity.ItemListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.ItemDetailActivity"
			android:label="@string/activity_label_detail">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.ItemListActivity" />
        </activity>

		<activity
			android:name=".activity.ItemCreateActivity"
			android:label="@string/activity_label_create">
			<intent-filter>
				<action android:name="android.intent.action.VIEW"/>
				<category android:name="android.intent.category.DEFAULT"/>
			</intent-filter>
		</activity>

    </application>

</manifest>
